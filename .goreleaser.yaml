project_name: pumperly-mcp
release:
  github:
    owner: GeiserX
    name: pumperly-mcp
  draft: false
  prerelease: auto # tag "v0.3.0-rc1" becomes pre-release

builds:
  - id: bin
    main: ./cmd/server
    goos: [linux, windows, darwin]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w
      - -X github.com/geiserx/pumperly-mcp/version.Version={{ .Version }}
      - -X github.com/geiserx/pumperly-mcp/version.Commit={{ .ShortCommit }}
      - -X github.com/geiserx/pumperly-mcp/version.Date={{ .Date }}

archives:
  - id: dist
    builds: [bin]
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: 'checksums.txt'

dockers:
  # ------------- amd64 -------------
  - image_templates:
      - "drumsergio/pumperly-mcp:{{ .Tag }}-amd64"
      - "drumsergio/pumperly-mcp:latest-amd64"
    dockerfile: Dockerfile
    goos:   linux
    goarch: amd64
    use:    buildx
    extra_files:
      - go.mod
      - go.sum
      - cmd/
      - internal/
      - client/
      - config/
      - version/
      - .goreleaser.yaml
    build_flag_templates:
      - "--platform=linux/amd64"
  # ------------- arm64 -------------
  - image_templates:
      - "drumsergio/pumperly-mcp:{{ .Tag }}-arm64"
      - "drumsergio/pumperly-mcp:latest-arm64"
    dockerfile: Dockerfile
    goos:   linux
    goarch: arm64
    use:    buildx
    extra_files:
      - go.mod
      - go.sum
      - cmd/
      - internal/
      - client/
      - config/
      - version/
      - .goreleaser.yaml
    build_flag_templates:
      - "--platform=linux/arm64"

docker_manifests:
  # stitch the per-arch images into a combined tag = multi-arch
  - name_template: 'drumsergio/pumperly-mcp:{{ .Tag }}'
    image_templates:
      - 'drumsergio/pumperly-mcp:{{ .Tag }}-amd64'
      - 'drumsergio/pumperly-mcp:{{ .Tag }}-arm64'
  - name_template: 'drumsergio/pumperly-mcp:latest'
    image_templates:
      - 'drumsergio/pumperly-mcp:latest-amd64'
      - 'drumsergio/pumperly-mcp:latest-arm64'

snapshot:
  name_template: "snapshot-{{ .ShortCommit }}"

changelog:
  groups:
    - title: "Features"
      regexp: '^feat'
    - title: "Fixes"
      regexp: '^fix'
